const PLUGIN_ID = "com.desain.persistence";
export const EXTENSION_NAME = "Persistent Tokens";

export const ATTACHED_TO_ROOT = "ROOT";

// Broadcasts
export const CHANNEL_MESSAGE = `${PLUGIN_ID}/message`;

// Storage
export const LOCAL_STORAGE_STORE_NAME = `${PLUGIN_ID}/localStorage`;
export const METADATA_KEY_PERSISTED = `${PLUGIN_ID}/persistedKeys`;

// Popover
export const ID_POPOVER_SETTINGS = `${PLUGIN_ID}/popoverSettings`;

// Context menu
export const ID_CONTEXTMENU_PERSIST = `${PLUGIN_ID}/contextMenu/persist`;
export const ID_CONTEXTMENU_SAVE_TEMPLATE = `${PLUGIN_ID}/contextMenu/saveTemplate`;
export const ID_CONTEXTMENU_RESET_TEMPLATE = `${PLUGIN_ID}/contextMenu/resetToTemplate`;
